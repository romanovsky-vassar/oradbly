# CircleCI 2.0 configuration file
#
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    filters:
      branches:
        only: master
    steps:
      - checkout

      - run:
          name: read file
          command: |
            foo="`cat README.md`"
            echo $foo

      - run:
          name: s2
          command: echo ${REPO_NAME} # config'd circleci project
